container_commands:
  01_db_migrate:
    command: "source /var/app/venv/*/bin/activate && python3 manage.py migrate"
    leader_only: true
  02_populate_projects:
    command: "source /var/app/venv/*/bin/activate && python3 manage.py populate_projects"
  03_populate_local_directory:
    command: "source /var/app/venv/*/bin/activate && python3 manage.py populate_local_directory"

#commands:
#  01_load_env_vars:
#    command: /opt/elasticbeanstalk/bin/get-config environment | jq -r 'to_entries | .[] | "export \(.key)=\"\(.value)\""' > /etc/profile.d/sh.local

packages:
  yum:
    jq: []

option_settings:
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: haritika_org.settings